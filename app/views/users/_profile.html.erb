<div id="user-profile">
	<h2><%= @user.name %></h2>
	<p><%= time_ago_in_words(@user.created_at) %></p>
	<% if @user.picture? %>
		<%= image_tag @user.picture.url, class: "user-image"  %>
	<% else %>	
		<%= image_tag "default.png", class: "user-image" %>
	<% end %>
</div>	

